ripple-alpha-lib-extensions
=====================

A collection of packages that extend the functionality of ripple-alpha-lib.

### Transaction Parser

Parses transaction objects to a higher-level view.
